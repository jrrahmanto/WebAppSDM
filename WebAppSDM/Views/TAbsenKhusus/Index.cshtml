@model IEnumerable<WebAppSDM.Models.TAbsenKhusus>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<script type="text/javascript">
    $(document).ready(function () {
        $.noConflict();
        $('#myDataTable').DataTable({});
    });
</script>

<div class="container-xxl flex-grow-1 container-p-y">
    <h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light">Absen/</span>Absen Khusus</h4>
    <!-- Basic Bootstrap Table -->
    <div class="card">
        <h5 class="card-header"> <a asp-action="create" class="btn btn-sm btn-outline-primary">Create</a></h5>
        <div class="card-header">

            <table class="table" id="myDataTable">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.nip)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.periode_start)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.periode_end)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.keterangan)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>

                            @if (item.nip == 999)
                            {
                                <td>
                                    Seluruh Pegawai
                                </td>
                            }
                            else
                            {
                                <td>
                                    @Html.DisplayFor(modelItem => item.nip)
                                </td>
                            }

                            <td>
                                @item.periode_start.ToString("dd/MM/yyyy")
                            </td>
                            <td>
                                @item.periode_end.ToString("dd/MM/yyyy")
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.keterangan)
                            </td>
                            <td>
                                <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
                                <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
